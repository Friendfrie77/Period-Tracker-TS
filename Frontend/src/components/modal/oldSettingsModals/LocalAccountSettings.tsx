import { AiOutlineClose } from "react-icons/ai";
import { functionType } from "../modals.types";

const LocalAccountSettings: React.FC = () =>{
    //allow user to delete all info from browser
    //allow user to export their data to either xls or xml
    const content = (
        <section>
            <div className ={`modal-box`}>
                <h1>test</h1>
            </div>
        </section>
    )
    return content
}
export default LocalAccountSettings